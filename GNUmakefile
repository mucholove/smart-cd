# Makefile for JustForFun Files

# A few variables

CC=clang-9
LIBS=-lncurses

# SRC_DIR=.
# EXE_DIR=../demo/exe

include $(GNUSTEP_MAKEFILES)/common.make

TOOL_NAME = FDD
FDD_OBJC_FILES = main.m

include GNUmakefile.preamble
include $(GNUSTEP_MAKEFILES)/tool.make


# ${EXE_DIR}/%: %.o
# 	${CC} -o $@ $< ${LIBS}
# 
# %.o: ${SRC_DIR}/%.c
# 	${CC} -o $@ -c $<
# 
# all:    ${EXES}
# 
# clean:
# 	@rm -f ${EXES}
